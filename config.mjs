// Add your domain name and ImprovMX API keys
export const improvmxApiKeys = {
    'example.com': 'sk_xxxxxxxxxxxxx',
};

// Lookback window to search for undelivered emails
export const logLookbackPeriod = 3 * 24 * 60 * 60 * 1000; // 3 days

// Slack bot token that has chat:write permission, and channel ID to post message
export const slackBotToken = 'xoxb-xxxxxxxxxxxxxxxxxxxxx';
export const slackChannelId = 'XXXXXXXXXXX';
